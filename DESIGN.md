# GRIS Scout — 设计规范（喜茶 · 黑白灰）

> 设计意象：一间极简的喜茶门店——纯白吧台、黑色极细手绘字、暖灰地砖、一盏柔和工作灯。
> 克制、留白、专注数据，不靠彩块制造“专业感”，靠黑白灰的层级与疏朗网格支撑信息密度。

## 气质与意象
- 主界面**白色**打底，近黑文字，细灰描边，浅灰分隔。整屏像一张被仔细排过版的「选址白纸」。
- 层级靠**色阶深浅**而非色彩区分：核心=近黑 `#111`，次要=中灰 `#6b7280`，弱化=浅灰 `#9ca3af`/`#d4d4d8`。
- 含蓄的“茶感”来自一点点暖调中性灰（如 `#f7f7f5` 画布、`#f3f4f6` 地图底），而非明亮配色。

## 视觉策略
- 地图为浅色网格底（`#f3f4f6` + 细黑格线），叠加单色图元，读起来像「黑白版分析底图」。
- 商圈/网格热力用**单色谱**：白→浅灰→深灰→近黑，仅靠明度表达浓度，避免花哨。
- 强调一律 `#111111`（激活状态、主按钮、图钉、标题）；辅助用灰阶区分，不用彩色标签。

## 配色方案
| Token | 十六进制 | 意象 |
|---|---|---|
| 画布底 | `#f7f7f5` | 暖白吧台 / 纸张 |
| 地图底 | `#f3f4f6` | 柔和瓷砖 |
| 主文字 / 强调 | `#111111` | 喜茶黑的手写墨 |
| 次级文字 | `#3f3f46` `#6b7280` | 贴标灰 |
| 弱化 / 占位 | `#9ca3af` `#d4d4d8` | 浅影 |
| 边框 / 描边 | `#e5e7eb` / `#d4d4d8` | 极细线 |
| 趋势 / 数据面色阶 | `#d4d4d8→#111111` | 单色浓度 |

商圈类型 / 客群图例也收敛为灰阶色档（核心商业近黑 → 办公深灰 → 社区中灰 → 旅游浅灰……）。

## 字体排版
- 正文 `Noto Sans SC / PingFang SC`：白底黑字，标题 900，正文 400–500。
- 数字用等宽 `font-num`（IBM Plex Mono / JetBrains Mono，tabular-nums），让分数、排名、人数对齐、可信。
- 不堆大标题，用字重与留白分主次。

## 动效与交互
- 克制：仅 hover 提亮、激活态黑白翻转、地图过渡与 heat 视野懒加载。
- 所有“选中/激活”统一为：**底反转黑、文字转白**，视觉反馈单一明确。

## 设计禁忌
- 不使用品牌色系（喜茶 logo 真色、翠绿、琥珀橙）大面积铺陈——强调色只能是黑。
- 不使用彩色大渐变卡片（hero 用 `#111→#3f3f46` 深灰渐变 + 白字）。
- 不为“好看”牺牲数据诚实：代理/演示数据必须用中性灰徽章明确标注，不可用“看起来真实”的配色暗示真实。
- 地图图元不追求多彩，越黑白灰越能突出“这个数字哪里来、可信不可信”。